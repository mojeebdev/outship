# Copy to .dev.vars for local `wrangler dev` / `next dev` runs.
# Never commit the real .dev.vars file — it's gitignored.

GITHUB_WEBHOOK_SECRET=
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_APP_CLIENT_ID=
GITHUB_APP_CLIENT_SECRET=
SESSION_SECRET=
